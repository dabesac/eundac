<ul class="nav nav-pills nav-stacked" id="backeditBtn">
	<li><a href="#" id="backedit"><h5>Volver</h5></a></li>
</ul>

<form id="form" action="/profile/public/studentnewlaboral" method="post">

<div class="col-sm-12">
	<table class="table">
		<tr>
			<td><label>Empresa</label><?php print $this->form->company?></td>
			<td><label>Area</label><?php print $this->form->industry?></td>
		</tr>
		<tr>
			<td><label>Salario Mensual</label><?php print $this->form->salary?></td>
			<td><label>Condición</label><?php print $this->form->condition?></td>
		</tr>
		<tr>
			<td></td>
			<td><a href="#" class="btn btn-info pull-right" id="save">Guardar</a></td>
		</tr>
	</table>
</div>


<script>
	$("#save").click(function(){
		$("#form").submit()
	})
	$("#form").submit(function(){
		$.ajax({
			type: "POST",
			url: $(this).attr("action"),
			data: $(this).serialize(),
			success: function(data){
				$("#editform").html(data);
				$("#editform").html('');
				$("#datastudent").removeClass("hidden");
				$('#datastudent').load("/profile/public/studentlaboral");
			}
		})
		return false;
	})

	$('#backeditBtn a').click(function (e) {
    	e.preventDefault()
      	$("#datastudent").removeClass('hidden');
      	$("#editform").html('');
  	})
   
</script>